- replace :layout, 'devise'

= mozaic do
  .myg-content.myg-grid__flex.-column.-center
    = link_to root_path, data: { turbolinks_animate_persist: 'devise' } do
      = asset 'logo_large.svg'
    .myg-card.myg-layout__wrap--phone{ data: { turbolinks_animate_persist_itself: 'devise' } }
      = mozaic_area :devise do
        = yield
