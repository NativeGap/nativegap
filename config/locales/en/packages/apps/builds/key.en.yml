en:
    apps:
        builds:
            key:
                title: Add signing key
                key_needed:
                    title: Add a signing key to get started
                    content: If you need help creating a signing key, reference the articles in the %{link}.
                    link: Help Center
                no_key_needed:
                    title: Update your signing key
                    content: Things have changed? You can add a new signing key, no problem.
                form:
                    no_file_chosen: No file chosen
                    android_key_alias: Key alias
                    android_key_password: Key password
                    android_keystore:
                        title: Keystore file
                        content: File format must be .keystore
                    android_keystore_password: Keystore password
                    ios_profile:
                        title: Provisioning Profile
                        content: File format must be .mobileprovision
                    ios_cert:
                        title: Certificate
                        content: File format must be .p12
                    ios_cert_password: Certificate password
                    add_signing_key: Add signing key
