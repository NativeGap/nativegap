language: ruby
rvm:
  - 2.5.1
script:
  - bundle exec rubocop
  - bundle exec rspec
  - yarn install --ignore-engines
  - bundle exec rails assets:precompile
before_install:
  - nvm install node 9.11.2
